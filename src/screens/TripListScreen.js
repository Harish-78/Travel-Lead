import {
  View,
  Text,
  StyleSheet,
  ScrollView,
  TouchableOpacity,
  Image,
} from 'react-native';
import React, {useState, useEffect} from 'react';
import Axios from 'axios';

const TripListScreen = () => {
  const [image, setImage] = useState('');
  const getimage = () => {};
  return (
    <View>
      <Text>TripListScreen</Text>
    </View>
  );
};

export default TripListScreen;
